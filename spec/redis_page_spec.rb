require 'spec_helper'

describe RedisPage do
  it 'has a version number' do
    expect(RedisPage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
